spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: trustid

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  access-expiration-ms: ${JWT_ACCESS_EXPIRATION_MS:7200000}
  refresh-expiration-ms: ${JWT_REFRESH_EXPIRATION_MS:86400000}

app:
  file-storage:
    upload-dir: ${APP_UPLOAD_DIR:../uploads}
  security:
    allowed-origins: ${APP_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
  qr:
    verify-base-url: ${APP_QR_VERIFY_BASE_URL:http://localhost:5173/public/verify}
    token-expiration-ms: ${APP_QR_TOKEN_EXPIRATION_MS:300000}
